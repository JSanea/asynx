# asynx

Build and run instructions:

```bash
mkdir -p build
cd build
cmake ..
cmake --build .
```

Notes:
- `CMakeCache.txt` and `CMakeFiles/` are generated in the `build/` directory; do not commit them.
